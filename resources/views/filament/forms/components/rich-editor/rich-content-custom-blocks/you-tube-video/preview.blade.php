<div class="rounded-xl border border-gray-200 bg-gray-50 p-4 text-sm text-gray-700">
    <p class="font-medium">{{ $title }}</p>

    @if ($watchUrl)
        <p class="mt-1 break-all text-xs text-gray-500">{{ $watchUrl }}</p>
    @endif
</div>
